; conference
; This module provides a conference bridge for multi-party calls.

[conference]
exten => s,1,Answer()
    same => n,ConfBridge(160)
    same => n,Hangup()

exten => #,1,Playback(goodbye)
    same => n,Hangup()
